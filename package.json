{
  "scripts": {
    "build": "pnpm -F=unplugin-auto-export run build",
    "publish": "pnpm -F=unplugin-auto-export run publish",
    "play:vite": "pnpm -F=vite-example run dev",
    "play:webpack": "pnpm -F=webpack-example run serve"
  }
}
